{
  "name": "diamond-rush-web", "version": "0.1.0", "private": true, "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test tests/*.test.ts",
    "audit": "node tools/audit.ts",
    "extract-assets": "node tools/extract.ts"
  },
  "devDependencies": {"@types/node": "^24.0.0", "typescript": "^5.9.0", "vite": "^7.1.0"}
}
